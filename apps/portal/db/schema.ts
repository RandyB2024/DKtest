import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const users = sqliteTable("users", { id: text("id").primaryKey(), email: text("email").notNull(), name: text("name").notNull(), functionTitle: text("function_title"), accountStatus: text("account_status").notNull().default("Uitgenodigd"), lastLogin: text("last_login"), lastOrganizationId: text("last_organization_id") }, table => [uniqueIndex("users_email_unique").on(table.email)]);
export const organizationGroups = sqliteTable("organization_groups", { id: text("id").primaryKey(), name: text("name").notNull() });
export const organizations = sqliteTable("organizations", { id: text("id").primaryKey(), groupId: text("group_id").references(() => organizationGroups.id), name: text("name").notNull(), legalForm: text("legal_form").notNull(), kvk: text("kvk").notNull(), status: text("status").notNull() }, table => [index("organizations_group_idx").on(table.groupId)]);
export const roles = sqliteTable("roles", { id: text("id").primaryKey(), name: text("name").notNull(), organizationId: text("organization_id").references(() => organizations.id), isSystem: integer("is_system", { mode: "boolean" }).notNull().default(false) }, table => [index("roles_org_idx").on(table.organizationId)]);
export const permissions = sqliteTable("permissions", { id: text("id").primaryKey(), code: text("code").notNull(), groupName: text("group_name").notNull(), label: text("label").notNull() }, table => [uniqueIndex("permissions_code_unique").on(table.code)]);
export const rolePermissions = sqliteTable("role_permissions", { roleId: text("role_id").notNull().references(() => roles.id), permissionId: text("permission_id").notNull().references(() => permissions.id) }, table => [uniqueIndex("role_permission_unique").on(table.roleId, table.permissionId)]);
export const memberships = sqliteTable("memberships", { id: text("id").primaryKey(), userId: text("user_id").notNull().references(() => users.id), organizationId: text("organization_id").notNull().references(() => organizations.id), role: text("role").notNull(), roleId: text("role_id").references(() => roles.id), status: text("status").notNull(), createdAt: text("created_at").notNull().default("1970-01-01"), createdBy: text("created_by").notNull().default("system").references(() => users.id), validFrom: text("valid_from").notNull().default("1970-01-01"), validUntil: text("valid_until"), additionalPermissions: text("additional_permissions") }, table => [uniqueIndex("memberships_user_org_unique").on(table.userId, table.organizationId), index("memberships_org_status_idx").on(table.organizationId, table.status), index("memberships_validity_idx").on(table.validFrom, table.validUntil)]);
export const activeOrganizationPreferences = sqliteTable("active_organization_preferences", { userId: text("user_id").primaryKey().references(() => users.id), organizationId: text("organization_id").notNull().references(() => organizations.id), updatedAt: text("updated_at").notNull() });
export const userInvitations = sqliteTable("user_invitations", { id: text("id").primaryKey(), email: text("email").notNull(), firstName: text("first_name").notNull(), lastName: text("last_name").notNull(), functionTitle: text("function_title"), status: text("status").notNull(), invitedAt: text("invited_at").notNull(), expiresAt: text("expires_at").notNull(), invitedBy: text("invited_by").notNull().references(() => users.id) }, table => [index("invitations_email_status_idx").on(table.email, table.status)]);
export const invitationMemberships = sqliteTable("invitation_memberships", { invitationId: text("invitation_id").notNull().references(() => userInvitations.id), organizationId: text("organization_id").notNull().references(() => organizations.id), role: text("role").notNull(), permissions: text("permissions"), validUntil: text("valid_until") }, table => [uniqueIndex("invitation_org_unique").on(table.invitationId, table.organizationId)]);
export const auditEvents = sqliteTable("audit_events", { id: text("id").primaryKey(), userId: text("user_id").notNull().references(() => users.id), organizationId: text("organization_id").references(() => organizations.id), action: text("action").notNull(), timestamp: text("timestamp").notNull(), objectType: text("object_type").notNull(), objectId: text("object_id").notNull(), result: text("result").notNull(), change: text("change"), sessionInfo: text("session_info") }, table => [index("audit_org_time_idx").on(table.organizationId, table.timestamp), index("audit_user_time_idx").on(table.userId, table.timestamp)]);
export const organizationRelations = sqliteTable("organization_relations", { id: text("id").primaryKey(), parentOrganizationId: text("parent_organization_id").notNull().references(() => organizations.id), childOrganizationId: text("child_organization_id").notNull().references(() => organizations.id), type: text("type").notNull(), ownershipPercentage: integer("ownership_percentage"), startDate: text("start_date").notNull(), endDate: text("end_date") }, table => [index("relations_parent_idx").on(table.parentOrganizationId), index("relations_child_idx").on(table.childOrganizationId)]);
export const invoices = sqliteTable("invoices", { id: text("id").primaryKey(), organizationId: text("organization_id").notNull().references(() => organizations.id), number: text("number").notNull(), status: text("status").notNull(), customer: text("customer").notNull() }, table => [index("invoices_org_idx").on(table.organizationId)]);
export const documents = sqliteTable("documents", { id: text("id").primaryKey(), organizationId: text("organization_id").notNull().references(() => organizations.id), filename: text("filename").notNull(), status: text("status").notNull() }, table => [index("documents_org_idx").on(table.organizationId)]);
export const returns = sqliteTable("returns", { id: text("id").primaryKey(), organizationId: text("organization_id").notNull().references(() => organizations.id), type: text("type").notNull(), period: text("period").notNull(), status: text("status").notNull() }, table => [index("returns_org_idx").on(table.organizationId)]);
export const vehicles = sqliteTable("vehicles", { id: text("id").primaryKey(), organizationId: text("organization_id").notNull().references(() => organizations.id), registration: text("registration").notNull() }, table => [index("vehicles_org_idx").on(table.organizationId)]);
export const reports = sqliteTable("reports", { id: text("id").primaryKey(), organizationId: text("organization_id").notNull().references(() => organizations.id), period: text("period").notNull(), updatedAt: text("updated_at").notNull() }, table => [index("reports_org_idx").on(table.organizationId)]);
export const insuranceRelationships = sqliteTable("insurance_relationships", { id: text("id").primaryKey(), customerId: text("customer_id").notNull(), organizationId: text("organization_id").references(() => organizations.id), status: text("status").notNull(), source: text("source").notNull(), externalRelationshipId: text("external_relationship_id").notNull(), validFrom: text("valid_from").notNull(), validUntil: text("valid_until"), linkedAt: text("linked_at").notNull(), linkedBy: text("linked_by").notNull(), verifiedAt: text("verified_at").notNull() }, table => [index("insurance_relationship_org_status_idx").on(table.organizationId, table.status), uniqueIndex("insurance_relationship_external_unique").on(table.source, table.externalRelationshipId)]);
export const insurers = sqliteTable("insurers", { id: text("id").primaryKey(), name: text("name").notNull(), emergencyPhone: text("emergency_phone") });
export const insurancePolicies = sqliteTable("insurance_policies", { id: text("id").primaryKey(), externalSourceId: text("external_source_id").notNull(), policyNumber: text("policy_number").notNull(), insurerId: text("insurer_id").notNull().references(() => insurers.id), organizationId: text("organization_id").references(() => organizations.id), personalUserId: text("personal_user_id").references(() => users.id), productType: text("product_type").notNull(), productName: text("product_name").notNull(), status: text("status").notNull(), startDate: text("start_date").notNull(), endDate: text("end_date"), contractRenewalDate: text("contract_renewal_date"), paymentTerm: text("payment_term").notNull(), premiumCents: integer("premium_cents"), source: text("source").notNull(), lastSynchronizedAt: text("last_synchronized_at").notNull() }, table => [uniqueIndex("insurance_policy_external_unique").on(table.source, table.externalSourceId), index("insurance_policy_org_status_idx").on(table.organizationId, table.status), index("insurance_policy_person_idx").on(table.personalUserId)]);
export const insuranceDocuments = sqliteTable("insurance_documents", { id: text("id").primaryKey(), policyId: text("policy_id").notNull().references(() => insurancePolicies.id), organizationId: text("organization_id").references(() => organizations.id), personalUserId: text("personal_user_id").references(() => users.id), name: text("name").notNull(), type: text("type").notNull(), documentDate: text("document_date").notNull(), version: text("version").notNull(), storageKey: text("storage_key").notNull(), source: text("source").notNull() }, table => [index("insurance_document_policy_idx").on(table.policyId), index("insurance_document_org_idx").on(table.organizationId)]);
export const insuranceChangeRequests = sqliteTable("insurance_change_requests", { id: text("id").primaryKey(), policyId: text("policy_id").notNull().references(() => insurancePolicies.id), organizationId: text("organization_id").references(() => organizations.id), requestedBy: text("requested_by").notNull().references(() => users.id), kind: text("kind").notNull(), effectiveDate: text("effective_date").notNull(), status: text("status").notNull(), createdAt: text("created_at").notNull() }, table => [index("insurance_change_org_status_idx").on(table.organizationId, table.status)]);
export const insuranceClaims = sqliteTable("insurance_claims", { id: text("id").primaryKey(), reference: text("reference").notNull(), externalClaimNumber: text("external_claim_number"), policyId: text("policy_id").notNull().references(() => insurancePolicies.id), organizationId: text("organization_id").references(() => organizations.id), personalUserId: text("personal_user_id").references(() => users.id), incidentAt: text("incident_at").notNull(), incidentType: text("incident_type").notNull(), status: text("status").notNull(), handler: text("handler") }, table => [uniqueIndex("insurance_claim_reference_unique").on(table.reference), index("insurance_claim_org_status_idx").on(table.organizationId, table.status), index("insurance_claim_person_idx").on(table.personalUserId)]);
export const claimTimelineEvents = sqliteTable("claim_timeline_events", { id: text("id").primaryKey(), claimId: text("claim_id").notNull().references(() => insuranceClaims.id), timestamp: text("timestamp").notNull(), status: text("status").notNull(), publicMessage: text("public_message").notNull(), actor: text("actor").notNull() }, table => [index("claim_timeline_claim_time_idx").on(table.claimId, table.timestamp)]);
export const insuranceConversations = sqliteTable("insurance_conversations", { id: text("id").primaryKey(), organizationId: text("organization_id").references(() => organizations.id), personalUserId: text("personal_user_id").references(() => users.id), policyId: text("policy_id").references(() => insurancePolicies.id), claimId: text("claim_id").references(() => insuranceClaims.id), subject: text("subject").notNull() }, table => [index("insurance_conversation_org_idx").on(table.organizationId), index("insurance_conversation_person_idx").on(table.personalUserId)]);
export const insuranceIntegrationLinks = sqliteTable("insurance_integration_links", { id: text("id").primaryKey(), relationshipId: text("relationship_id").notNull().references(() => insuranceRelationships.id), system: text("system").notNull(), externalId: text("external_id").notNull(), lastSynchronizedAt: text("last_synchronized_at").notNull() }, table => [uniqueIndex("insurance_integration_external_unique").on(table.system, table.externalId)]);
export const insuranceReferrals = sqliteTable("insurance_referrals", { id: text("id").primaryKey(), customerId: text("customer_id").notNull(), organizationId: text("organization_id").references(() => organizations.id), origin: text("origin").notNull(), status: text("status").notNull(), createdAt: text("created_at").notNull() }, table => [index("insurance_referral_org_status_idx").on(table.organizationId, table.status)]);
export const communicationThreads = sqliteTable("communication_threads", { id:text("id").primaryKey(), organizationId:text("organization_id").notNull().references(()=>organizations.id), createdBy:text("created_by").notNull().references(()=>users.id), subject:text("subject").notNull(), category:text("category").notNull(), status:text("status").notNull(), assignee:text("assignee"), priority:text("priority").notNull().default("Normaal"), deadline:text("deadline"), archived:integer("archived",{mode:"boolean"}).notNull().default(false), idempotencyKey:text("idempotency_key") },t=>[index("communication_org_status_idx").on(t.organizationId,t.status),uniqueIndex("communication_idempotency_unique").on(t.idempotencyKey)]);
export const communicationMessages = sqliteTable("communication_messages", { id:text("id").primaryKey(), threadId:text("thread_id").notNull().references(()=>communicationThreads.id), senderId:text("sender_id").notNull(), kind:text("kind").notNull(), body:text("body").notNull(), createdAt:text("created_at").notNull(), customerVisible:integer("customer_visible",{mode:"boolean"}).notNull().default(true) },t=>[index("communication_message_thread_time_idx").on(t.threadId,t.createdAt)]);
export const billingProfiles = sqliteTable("billing_profiles", { organizationId:text("organization_id").primaryKey().references(()=>organizations.id), companyName:text("company_name").notNull(), tradeName:text("trade_name").notNull(), address:text("address").notNull(), postalCode:text("postal_code").notNull(), city:text("city").notNull(), country:text("country").notNull(), chamberOfCommerce:text("chamber_of_commerce").notNull(), vatId:text("vat_id").notNull(), iban:text("iban").notNull(), bic:text("bic"), email:text("email").notNull(), phone:text("phone"), website:text("website"), paymentTermDays:integer("payment_term_days").notNull(), defaultVatRate:integer("default_vat_rate").notNull(), invoicePrefix:text("invoice_prefix").notNull(), nextNumber:integer("next_number").notNull(), fiscalScheme:text("fiscal_scheme").notNull(), logoStorageKey:text("logo_storage_key") });
export const debtors = sqliteTable("debtors", { id:text("id").primaryKey(), organizationId:text("organization_id").notNull().references(()=>organizations.id), type:text("type").notNull(), displayName:text("display_name").notNull(), address:text("address").notNull(), postalCode:text("postal_code").notNull(), city:text("city").notNull(), country:text("country").notNull(), email:text("email").notNull(), customerNumber:text("customer_number").notNull(), paymentTermDays:integer("payment_term_days").notNull(), vatTreatment:text("vat_treatment").notNull(), archived:integer("archived",{mode:"boolean"}).notNull().default(false) },t=>[index("debtors_org_archived_idx").on(t.organizationId,t.archived),uniqueIndex("debtors_org_customer_number_unique").on(t.organizationId,t.customerNumber)]);
export const professionalInvoices = sqliteTable("professional_invoices", { id:text("id").primaryKey(), organizationId:text("organization_id").notNull().references(()=>organizations.id), debtorId:text("debtor_id").notNull().references(()=>debtors.id), number:text("number"), invoiceDate:text("invoice_date").notNull(), dueDate:text("due_date").notNull(), reference:text("reference"), status:text("status").notNull(), paidCents:integer("paid_cents").notNull().default(0), finalizedAt:text("finalized_at"), sentAt:text("sent_at"), originalInvoiceId:text("original_invoice_id"), accountingStatus:text("accounting_status").notNull(), pdfStorageKey:text("pdf_storage_key"), immutableSnapshot:text("immutable_snapshot") },t=>[uniqueIndex("invoice_org_number_unique").on(t.organizationId,t.number),index("invoice_org_status_date_idx").on(t.organizationId,t.status,t.invoiceDate),index("invoice_debtor_idx").on(t.debtorId)]);
export const professionalInvoiceLines = sqliteTable("professional_invoice_lines", { id:text("id").primaryKey(), invoiceId:text("invoice_id").notNull().references(()=>professionalInvoices.id), description:text("description").notNull(), quantityMinor:integer("quantity_minor").notNull(), unit:text("unit").notNull(), unitPriceCents:integer("unit_price_cents").notNull(), discountBasisPoints:integer("discount_basis_points").notNull().default(0), vatRateBasisPoints:integer("vat_rate_basis_points").notNull(), category:text("category") },t=>[index("professional_invoice_lines_invoice_idx").on(t.invoiceId)]);
export const invoiceNumberSequences = sqliteTable("invoice_number_sequences", { organizationId:text("organization_id").notNull().references(()=>organizations.id), year:integer("year").notNull(), nextNumber:integer("next_number").notNull() },t=>[uniqueIndex("invoice_sequence_org_year_unique").on(t.organizationId,t.year)]);
export const fileAssets = sqliteTable("file_assets", { id:text("id").primaryKey(), organizationId:text("organization_id").notNull().references(()=>organizations.id), ownerType:text("owner_type").notNull(), ownerId:text("owner_id").notNull(), storageKey:text("storage_key").notNull(), filename:text("filename").notNull(), contentType:text("content_type").notNull(), size:integer("size").notNull(), createdBy:text("created_by").notNull().references(()=>users.id), createdAt:text("created_at").notNull() },t=>[index("file_assets_owner_idx").on(t.organizationId,t.ownerType,t.ownerId)]);
