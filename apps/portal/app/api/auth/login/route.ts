import { NextResponse } from "next/server";
export async function POST(request: Request) { const body = await request.json().catch(() => ({})) as { email?: string; password?: string }; const accounts: Record<string, { password: string; userId: string }> = { "demo@destinationknown.test": { password: "Welkom2026!", userId: "user-randy" }, "ed@example.test": { password: "Welkom2026!", userId: "user-ed" } }; const account = body.email ? accounts[body.email] : undefined; if (!account || account.password !== body.password) return NextResponse.json({ error: "E-mailadres of wachtwoord klopt niet." }, { status: 401 }); const response = NextResponse.json({ ok: true }); response.cookies.set("mdk_session", account.userId, { httpOnly: true, sameSite: "strict", secure: process.env.NODE_ENV === "production", path: "/", maxAge: 28800 }); return response; }
