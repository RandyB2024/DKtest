{
  "name": "destination-known-office-pwa",
  "version": "0.1.0",
  "private": true,
  "description": "Lokale PWA-MVP voor Destination Known Office",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
